import { Pushable } from 'it-pushable'

export const handleVoucher = (writeStream: Pushable<any>, message) => {
  // const request = createRequest()
  // console.log('sending message:', request)

  // writeStream.push(request)
}
